apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: mongodb
spec:
  serviceName: mongodb-service
  replicas: 1 
  selector:
    matchLabels:
      app: mongodb
  template:
    metadata:
      labels:
        app: mongodb
    spec:
      containers:
        - name: mongodb
          image: mongo:latest  # Official MongoDB image
          ports:
            - containerPort: 27017  # MongoDB default port
          env:
            - name: MONGO_INITDB_ROOT_USERNAME
              value: "root"  # user
            - name: MONGO_INITDB_ROOT_PASSWORD
              value: "rootpassword"  #password
            - name: MONGO_INITDB_DATABASE
              value: "your-database-name"  # database name
          volumeMounts:
            - name: mongodb-data
              mountPath: /data/db
  volumeClaimTemplates:
    - metadata:
        name: mongodb-data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 1Gi  # storage size
---
apiVersion: v1
kind: Service
metadata:
  name: mongodb-service
spec:
  selector:
    app: mongodb
  ports:
    - protocol: TCP
      port: 27017
      targetPort: 27017
  clusterIP: None  # Headless service to maintain stable network identities
